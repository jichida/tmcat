version: '2'
services:
    appserver:
      image: puppeteer-chrome-linux:latest
      restart: always
      command: node ./test.js
      working_dir: /app
      volumes:
        - ./:/app
      network_mode: "host"
